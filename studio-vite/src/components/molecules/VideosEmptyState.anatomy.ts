import type { ComponentMeta } from '../../design-system/types'
import { VideosEmptyState } from './VideosEmptyState'

export const meta: ComponentMeta = {
  id: 'videos-empty-state',
  label: 'VideosEmptyState',
  category: 'molecules',
  description: 'Empty state placeholder shown when no video sessions are available.',
  component: VideosEmptyState,
  anatomy: [],
}
