import type { ComponentMeta } from '../../design-system/types'
import { AgentPageHeaderDemo } from './AgentPageHeader.demo'

export const meta: ComponentMeta = {
  id: 'agent-page-header',
  label: 'AgentPageHeader',
  category: 'molecules',
  description: 'Large agent identity block: 72px gradient icon + title (32px) + description. Used at the top of dedicated agent pages.',
  demo: AgentPageHeaderDemo,
  anatomy: [],
}
