import type { ComponentMeta } from '../../design-system/types'

export const meta: ComponentMeta = {
  id: 'language-selector',
  label: 'LanguageSelector',
  category: 'molecules',
  description: 'Language selector overlay. Opens to the right of the sidebar footer on profile click. Requires RefObject anchor — not auto-renderable.',
  anatomy: [],
}
