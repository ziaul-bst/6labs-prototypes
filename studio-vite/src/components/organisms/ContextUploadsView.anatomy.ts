import type { ComponentMeta } from '../../design-system/types'

export const meta: ComponentMeta = {
  id: 'context-uploads-view',
  label: 'ContextUploadsView',
  category: 'organisms',
  description: 'Full Uploads page with empty state, uploading state (progress), and uploaded state (description + save).',
  anatomy: [],
}
